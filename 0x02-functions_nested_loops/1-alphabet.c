#include "main.h"
/**
 * main - Entry point
 * Return:0
 */
int main(void)
{
    char characters = 'a';
    while(characters <= 'z')
    {
        _putchar(characters);
        characters++;
    }
    _putchar('\n');
    return 0;
}

