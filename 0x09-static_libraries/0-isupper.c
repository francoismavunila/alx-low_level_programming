#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
        if((c>=98) && (c<=122))
        {
            return 0;
        }else
        {
            return 1;
        }
}
